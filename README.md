# Smart-Inventory-Predictor
A full-stack enterprise integration project linking a Python Analytics Engine (Pandas, NumPy) to a Java Spring Boot REST API via HTTP POST. Computes inventory sales velocity (Vm) and run-out rates (Ro), predicts 7-day demand, generates CRITICAL_RESTOCK_WARN alerts, and persists data with Spring Data JPA.
